pointeur pour init et pointeur pour new windows



mlx_init () renvoie NULL si ca fail

-------------------------------------------------------------------------

mlx_new_window (void *mlx_ptr, int size_x, int size_y, char *title)  renvoie NULL si ca fail 

param : le pointeur mlx_init, la taille x et y , le titre de la fenetre

-------------------------------------------------------------------------

mlx_clear_window et mlx_destroy_window (void *mlx_ptr, void *win_ptr)

param : le pointeur init et le pointeur windows

-------------------------------------------------------------------------

mlx_loop (void *mlx_ptr)

le pointeur init, boucle infinie